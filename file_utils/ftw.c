#include <stdio.h>

int main( int argc, char *argv[] )
{
   if( argc != 2)
   {
      printf( "ftw prints information about files in specified directory\n");
      printf( "Usage: %s <dir>\n", argv[0] );
      return 0;
   }

   
   
   return 0;
}